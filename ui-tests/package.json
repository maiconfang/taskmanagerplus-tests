{
  "name": "taskmanagerplus-ui-tests",
  "version": "1.0.0",
  "description": "UI automated tests for TaskManagerPlus using Playwright and TypeScript (POM). Generates JSON reports for AI analysis.",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf test-results reports/ui",
    "test": "playwright test",
    "test:ci": "npm run clean && playwright test",
    "test:failures": "playwright test tests/failure-scenarios",
    "test:failures:ui": "playwright test tests/failure-scenarios/ui",
    "test:failures:api": "playwright test tests/failure-scenarios/api"
  },
  "keywords": [
    "qa",
    "playwright",
    "typescript",
    "automation",
    "ui-tests",
    "taskmanagerplus"
  ],
  "author": "Maicon Fang",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "@types/node": "^24.10.11",
    "rimraf": "^6.0.1"
  }
}